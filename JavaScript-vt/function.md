# 関数

```js
function 関数名(引数1, 引数2, ...) {
    // 処理

    // 戻り値がある場合
    return 戻り値;
}
```

ここもほぼPHPと同じ

※ この時、引数には `let` や `const` は不要  
→ 関数の引数はブロックスコープなので、自動でJavaScriptが補完する  
逆に、記述するとエラーになるので注意  
